<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AlphabetHeader" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/description_text_size</item>
        <item name="android:gravity">center</item>
        <item name="android:height">30dp</item>
        <item name="android:background">@drawable/alphabet_table_label_background</item>
        <item name="colorAccent">@color/white</item>
    </style>

    <style name="AlphabetEditText" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/description_text_size</item>
        <item name="android:gravity">center</item>
        <item name="android:height">50dp</item>
        <item name="android:background">@drawable/alphabet_table_label_background</item>
        <item name="colorAccent">@color/white</item>
    </style>
</resources>